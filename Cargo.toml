[package]
name = "asc"
version = "1.0.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "High-performance Container Runtime (Android Systemd Container)"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
